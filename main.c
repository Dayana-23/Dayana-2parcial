#include <stdio.h>

int main(){
    float a,b,x;
    printf("Valores de a y b");
    scanf("%f", &a);
    scanf("%f", &b);
    x= -b / a;
    printf("x = %.2f",x);
    return 0;
 // mas que nada fue hacer calculos e interpretar eso en codigo  como ingresar las variables y solo dividirlas
}
