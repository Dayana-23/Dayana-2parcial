#include<stdio.h>
 int main(){
int a,b,s;
printf("Ingresa dos numeros: ");
scanf("%d %d", &a,&b);
s = a+b;
if(s>0)
printf("Suma positiva");
else if(s<0)
printf("Suma negativa");
else 
printf("Suma cero");
    \\ suma de dos numeros e indica si es positiva o negativo 
 }