#include <stdio.h>
#include <math.h>
int main() {
    float a, b, h;
    printf("Catetos: ");
    scanf("%f %f", &a, &b);
    h = sqrt(a*a + b*b);
    printf("Hipotenusa: %.2f", h);
    return 0;
}